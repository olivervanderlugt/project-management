# Reference

Notes and links worth keeping. No structure enforced here — one file per topic,
named after the topic.

Frontmatter is optional. If a note is about a decision, it belongs in
`decisions/` instead. If it has a next action, it belongs in `projects/`.
